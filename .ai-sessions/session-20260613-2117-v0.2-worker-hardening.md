# Session Summary: Integrate v0.2 §29 (worker hardening)

**Date**: 2026-06-13
**Model**: Opus 4.8

## Key Actions

- Integrated the worker-hardening contracts into spec.md as §29 (Phase 10), four
  subsections: worker versioning (29.1), slot suppliers/tuner (29.2), autoscaling
  pollers (29.3), determinism enforcement (29.4). Added plan/todo steps P10.5–P10.8.
- Resolved the draft's twelve flagged decisions (WH-1..12) inline:
  deployment-primary versioning with legacy build-id deprecated; ship the per-workflow
  :VersioningBehavior attribute (scenarios need it); defer the legacy build-id client
  RPCs; no worker-deployment management client API (matches both refs); build_id default
  = MD5 of sorted %INC; omit worker_deployment_version from ReserveContext (absent at the
  pin); ship 4 slot pools incl. custom; ship nexus poller behavior; Python poller-override
  semantics; determinism guard best-effort (CORE::GLOBAL:: overrides, ON by default, with
  documented gaps).
- Custom slot suppliers are the only v0.2 feature needing NEW Rust shim work
  (reserve/try_reserve/mark/release callbacks on Tokio threads → queue drain +
  complete_async_reserve).

## Observations

- Spec now §18–§29. Remaining Phase 10: §30 client extras (reset + http_proxy), §31
  env-config. Then features-harness spec + samples-perl spec. Drafts in .v0.2-drafts/
  (env-config draft already written by the background agent).

## Suggested Skills for Next Session

- None special.
