# Session Summary: Apply adversarial-review fixes to the v0.2 spec/plan

**Date**: 2026-06-21
**Model**: Opus 4.8

## Key Actions

- Ran an adversarial reviewer (Temporal skill + docs MCP + in-repo oracles) over the v0.2
  spec/plan, then **independently validated its findings** against the protos, C header,
  and reference SDK source before acting. Validation caught the reviewer making one of
  its own CRITICAL findings wrong.
- Applied the validated corrections to spec.md + plan.md + todo.md (one commit):

**C1 (real, my error):** §31 env-config rested on a false premise. The C bridge DOES
expose `temporal_core_client_env_config_load`/`_profile_load` (header :918/:925). Rewrote
§31 to call the FFI and parse its JSON, dropping the pure-Perl envconfig.rs port + the
TOML::Tiny/per-OS-path-helper deps. Updated P10.10 + todo.

**M1 (real, user decision = match references):** §19 claimed Perl "follows sdk-python's
hard gate" for in-flight handlers. Both Python (`_warn_if_unfinished_handlers`) and Ruby
are warn-and-complete; the Perl v0.1 runner hard-blocks. Re-documented §19 to warn-and-
complete + expose `all_handlers_finished`; added a P6.2 step to relax the v0.1 hard gate.

**C2 (reviewer was WRONG):** it claimed the §26.4 gRPC→Nexus rows for UNIMPLEMENTED/
DEADLINE_EXCEEDED were invented; `_nexus.py:562-570` has both verbatim. Kept the rows;
added the omitted non-retryable group for completeness, citing the source.

**M2/M3/M4 + minors:** narrowed §29.4 determinism overrides to the time/entropy surface
(dropped open/fork/kill/system/exec); added a required reentrancy spike to §28.2/P10.4
for the metric-meter condvar; reframed §25's schedule-action field rejection as SDK
convention not proto fact; fixed §11 test-ID hyphenation (CI-grep parity), the T-wkrver
1..9 plan coverage, and the §32 phantom header.

## Observations

- Validating the validator was essential: C2 would have sent me to "fix" a correct table.
  The spec was otherwise accurate on the dense MUST-match constants.

## Suggested Skills for Next Session

- None special. The v0.2 spec/plan are now review-clean and ready for execution.
