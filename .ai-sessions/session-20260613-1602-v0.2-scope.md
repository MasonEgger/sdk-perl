# Session Summary: Scope v0.2 feature parity in the spec

**Date**: 2026-06-13
**Duration**: ~short (planning + spec scaffolding)
**Conversation Turns**: ~4
**Estimated Cost**: low-moderate
**Model**: Opus 4.8

## Key Actions

- Established the v0.2 milestone in `spec.md`: feature parity with the mature
  SDKs (Python/Ruby). Added Phase 6–10 acceptance criteria to §11 and trimmed §15
  to what truly stays deferred.
- Confirmed three scope decisions with the user: I author the v0.2 spec directly
  (grounded in Python/Ruby source); keep build-from-source (no prebuilt binaries);
  drop Sessions (Go/Java-only, not part of Python/Ruby parity).
- Verified parity targets against the reference SDK layouts
  (`sdk-python/temporalio/workflow/*`, `sdk-ruby/temporalio/lib/*`).

## Phase 6–10 plan (v0.2)

- **6** Workflow parity: child workflows, updates (`:Update`/`:UpdateValidator` +
  client `execute_update`/`start_update`), external-workflow handles.
- **7** Activity parity: local activities, async activity completion.
- **8** Scheduling: schedules + ScheduleHandle.
- **9** Nexus: caller + handler sides.
- **10** Runtime/worker hardening: core→Perl log forwarding (7th shim trampoline),
  custom metric meters, custom slot suppliers / autoscaling / deployment-based
  worker versioning, determinism enforcement.

## Prompt Inventory

| Prompt/Command | Action Taken | Outcome |
|---|---|---|
| "v0.2 = everything not implemented, minus Windows + CPAN" | Scoped v0.2, asked 3 forking questions | Scope confirmed |
| (decisions) author-direct / source-build / drop-sessions | Framed Phases 6–10 in spec §11, trimmed §15 | Committed scope |

## Observations

- The detailed per-feature contracts (§17+) are the main remaining work — roughly
  the scale of the original v0.1 spec. Each section must be authored from the
  reference SDK source (§0 prime directive) before its TDD plan is generated.
- The authoring is highly parallelizable (independent feature areas), so a
  multi-agent workflow is a candidate for accelerating the drafting — offered to
  the user as a throughput choice.
- `/bpe:plan` regenerates from the whole spec, so v0.2 plan generation needs the
  v0.1 `plan.md`/`todo.md` archived (or a scoped regeneration) — to be handled once
  the §17+ sections exist.

## Suggested Skills for Next Session

- `bpe:plan` once the §17+ contracts are authored (to generate the v0.2 plan/todo).
