# Session Summary: Integrate v0.2 Phase 7 (activity parity) contracts

**Date**: 2026-06-13
**Model**: Opus 4.8

## Key Actions

- Integrated the Phase 7 contracts into spec.md from the research drafts: §22 async
  activity completion, §23 eager start, §24 in-workflow upsert search-attributes/memo
  (§21 local activities landed in the prior commit). Added plan/todo steps P7.2–P7.4.
- Resolved the drafts' flagged decisions inline: eager workflow start is **detect-only**
  (no C-bridge eager API; `eagerly_started` accessor, core owns dispatch); keyword-only
  async-activity factory; `complete_async` package verb + public `CompleteAsync` class;
  **typed-only** SA upsert (`value_set`/`value_unset`, reject Python's deprecated dict);
  `AsyncActivityCancelled` spelling (double-L, matches existing `Cancelled`).

## Observations

- Phase 7 spec is complete (§21–§24). Remaining v0.2 integration: Phase 8 (schedules),
  Phase 9 (Nexus caller+handler), Phase 10 (interceptors/tracing, observability, worker
  hardening, client extras, env-config), then the features-harness spec and samples-perl
  spec. All drafts are in `.v0.2-drafts/`.

## Suggested Skills for Next Session

- None special.
